apiVersion: v1
kind: PersistentVolume
metadata:
  labels:
    volume: mongodb-data
  name: mongodb-data
  namespace: kryption
spec:
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 10Gi
